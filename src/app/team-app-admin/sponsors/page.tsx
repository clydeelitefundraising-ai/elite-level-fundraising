// @deprecated — prototype. Superseded by /admin/sponsors. See
// /team-app-admin/page.tsx.
import { redirect } from "next/navigation";

export default function DeprecatedTeamAppAdminSponsors() {
  redirect("/admin");
}
